
define host{
        use linux-server
        host_name {{ instance_name }}
        alias {{ instance_name }}
        address {{ ansible_default_ipv4.address }}
}

define service {
        use ping
        host_name {{ instance_ name }}
}
